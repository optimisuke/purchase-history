query GetOrders($id: Int!) {
  customer(id: $id) {
    order {
      lineitem {
        product {
          id
          image
          title
        }
      }
      id
      createdat
    }
    id
    name
  }
}
